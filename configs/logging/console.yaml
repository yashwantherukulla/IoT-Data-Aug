# ================================================================= #
#  Logging Configuration - Console (default)                         #
# ================================================================= #

backend: console
log_every_n_steps: 10
save_dir: "outputs/logs"

# ---- W&B stub (copy to wandb.yaml and fill in) ------------------
# backend: wandb
# project: cgdap_har
# entity: your_wandb_entity
# log_every_n_steps: 10
# save_dir: "outputs/logs"
